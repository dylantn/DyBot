const Discord = require("discord.js")

module.exports = {
    name: 'voice',
    description: 'joins voice channel',
    execute(message, args){
        // Obtained using:
    voiceChannel.join()
    .then(connection => {

    });
        }
    }